# Google OAuth credentials (for @example.com identity validation)
# Store in GCP Secret Manager: slides-oauth-id, slides-oauth-secret
GOOGLE_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-oauth-client-secret

# Server settings
PORT=8080
ALLOWED_DOMAIN=example.com

# Update this after first Cloud Run deploy — used in OAuth metadata responses.
SERVER_URL=https://your-service.example.com

# Bulk-operation Slack alerting (Slack bot token, chat:write only).
# A single call modifying more than BULK_OP_THRESHOLD objects DMs the alert user.
BULK_OP_THRESHOLD=20
BULK_OP_ALERT_USER_ID=YOUR_SLACK_USER_ID
SLACK_BOT_TOKEN=xoxb-...
